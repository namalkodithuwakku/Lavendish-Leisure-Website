import type { Metadata } from "next";
import { ContactActions, MobileDock, SiteFooter, SiteHeader } from "@/components/SiteChrome";
import { groupContact, hotels, whatsappLink } from "@/lib/hotels";
export const metadata:Metadata={title:"Contact & Reservations | Lavendish Leisure",description:"Call, WhatsApp or book Lavendish Leisure hotels across Sri Lanka. Find direct contact information for all ten hotels."};
export default function ContactPage(){return <main><SiteHeader/><section className="inner-hero contact-hero"><p className="eyebrow">CONTACT & RESERVATIONS</p><h1>Let’s plan your stay.</h1><p>Speak directly with our team or contact your chosen hotel.</p></section><section className="contact-lead"><ContactActions/><div className="contact-copy"><h2>Central reservations</h2><p>Landline: +94 11 208 8865<br/>Travel Agent Sales: +94 11 7555 885<br/>Email: <a href={`mailto:${groupContact.email}`}>{groupContact.email}</a></p></div></section><section className="hotel-directory"><div className="section-title"><p className="eyebrow purple">DIRECT HOTEL CONTACTS</p><h2>All properties</h2></div><div>{hotels.map(h=><article key={h.slug}><p>{h.destination}</p><h3>{h.name}</h3><span>{h.address}</span><a href={`tel:${h.phone}`}>Call {h.phoneDisplay}</a><a href={whatsappLink(h)}>WhatsApp</a></article>)}</div></section><SiteFooter/><MobileDock/></main>}
